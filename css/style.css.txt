/* 기존 디자인 그대로 정리 */
:root {
  --primary-navy:#0A2342; --accent-gold:#C5A47E; --danger-red:#D32F2F;
  --bg-light:#F8F9FA; --card-bg:#FFF; --border:#E1E5E9;
}
body{margin:0;font-family:Pretendard,sans-serif;background:var(--bg-light)}
.container{max-width:950px;margin:auto;padding:10px}
.active-view{display:block} #view-detailed{display:none}
.header-card{background:#fff;border-radius:20px;padding:20px;margin-bottom:15px}
.header-top{display:flex;justify-content:space-between;align-items:center}
.logo-box{display:flex;align-items:center;gap:12px}
.logo-img{width:48px;height:auto}
.logo-text h1{margin:0;color:var(--primary-navy)}
.logo-text span{font-size:12px;color:var(--accent-gold)}
.rep-info strong{display:block}
.contact-highlight{color:var(--danger-red);font-weight:900}
.notice-banner{background:#fff3f3;color:#c0392b;padding:12px;border-radius:12px;margin-bottom:10px;text-align:center}
.info-grid{display:grid;grid-template-columns:1fr 1fr;gap:15px}
.full-row{grid-column:span 2}
.input-field{width:100%;padding:12px;border-radius:10px;border:2px solid var(--border)}
.estimate-section{background:#fff;border-radius:20px;margin-bottom:20px}
.grid-header{background:var(--accent-gold);color:#fff;padding:12px;display:grid}
.master-grid{grid-template-columns:3fr 1fr 1fr 1fr}
.detail-grid{grid-template-columns:2fr 3fr .7fr 1.3fr 1.3fr}
.total-card{text-align:center;background:linear-gradient(135deg,var(--accent-gold),#b3936e);color:#fff;padding:30px;border-radius:20px}
.total-val{font-size:40px;font-weight:900}
.btn-action{padding:20px;border:none;border-radius:12px;font-size:18px;font-weight:800;width:100%;margin-top:10px;cursor:pointer}
.btn-red{background:var(--danger-red);color:#fff}
.btn-navy{background:var(--primary-navy);color:#fff}
.btn-gold{background:var(--accent-gold);color:#fff}